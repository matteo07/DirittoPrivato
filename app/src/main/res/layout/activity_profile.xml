<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/white"
    tools:context="com.exam.pier.dirittoprivato.dir.exam.pier.dirittoprivato.ProfileActivity"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:weightSum="1">
    <TextView
        android:text="@string/quiz_svolti_textView"
        android:id="@+id/quiz_svolti"
        android:layout_height="80dp"
        android:gravity="left|center"
        android:textColor="@color/cyan500"
        android:paddingLeft="15dp"
        android:layout_width="250dp" />

        <android.support.design.widget.FloatingActionButton
            android:onClick="deleteStas"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:clickable="true"
            app:fabSize="normal"
            app:srcCompat="@android:drawable/ic_menu_delete"
            android:id="@+id/deleteStas"
            app:backgroundTint="@color/teal100" />
    </LinearLayout>

    <ListView

        android:footerDividersEnabled="false"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/stats"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/quiz_svolti"
        android:clickable="false"
        />



</LinearLayout>
